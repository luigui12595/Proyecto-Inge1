
@model PagedList.IPagedList<ProyectoInge1.Models.HistVersiones >
@using PagedList.Mvc;
<link href="~/Content/PagedList.css" rel="stylesheet" type="text/css" />
@using Microsoft.AspNet.Identity
@using Microsoft.AspNet.Identity.Owin;
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Historial de Versiones</h2>


@using (Html.BeginForm("Index", "GestCambios", FormMethod.Get))
{
    <p>
        <div class="row">

            <div class="col-lg-4">
                <label class="control-label col-md-4">Proyectos:</label>
                @Html.TextBox("SearchString", ViewBag.CurrentFilter as string)



                              @*
        @Html.DropDownListFor(model => model.proyecto.lider, (SelectList)ViewBag.Desarrolladores, "Seleccionar Lider", new { @class = "control-label col-md-2" } );
           @{
            foreach (var Proj in Model )
            {
                        <option value=Proj.ReqFuncional.nomProyecto>
                            @Html.DropDownListFor(, new[] { new SelectListItem() { Text = Proj.ReqFuncional.nomProyecto.ToString(), Value = Proj.ReqFuncional.nomProyecto } }, "--Fuente no asignada--", new { @class = "form-control" })

                            @Html.DropDownList("Prueba", new[] { new SelectListItem() { Text = Proj.ReqFuncional.nomProyecto.ToString(), Value = Proj.ReqFuncional.nomProyecto } }, "--Escoger un Proyecto--", new { @class = "form-control" })
                        </option>
            }
        }*@
                <input class="btn btn-success" type="submit" value="Buscar" />
            </div>
            <div class="col-lg-4">
                <label class="control-label col-md-4">Requerimientos:</label>
                @Html.TextBox("SearchString", ViewBag.CurrentFilter as string)
             @*   @{
                    foreach (var Proj in Model)
                    {
                        <option value=Proj.ReqFuncional.nomProyecto>
                            @Html.DropDownListFor(, new[] { new SelectListItem() { Text = Proj.ReqFuncional.nomProyecto.ToString(), Value = Proj.ReqFuncional.nomProyecto } }, "--Fuente no asignada--", new { @class = "form-control" })
                            
                            @Html.DropDownList("Prueba", new[] { new SelectListItem() { Text = Proj.ReqFuncional.nombre.ToString(), Value = Proj.ReqFuncional.nombre} }, "--Escoger un Requerimiento--", new { @class = "form-control" })
                        </option>
                    }
                }*@
                <input class="btn btn-success" type="submit" value="Buscar" />
            </div>

            <div class="col-lg-1 col-lg-pull-1">
                <a class="btn btn-info" href="../GestCambios">Mostrar Todos</a>
            </div> 
            <div class="col-lg-1 col-lg-pull-1">
                <a class="btn btn-info" href="../GestCambios/Solicitudes">Solicitudes</a>
            </div>
        </div>
    </p>
}


<table id="table1" class="table table-striped table-bordered table-hover">
    <thead>
        <tr>
            <th>
                @Html.ActionLink("Version", "Index", new { sortOrder = ViewBag.NameSortParm, currentFilter = ViewBag.CurrentFilter })
            </th>
            <th>
                @Html.ActionLink("Requerimiento", "Index", new { sortOrder = ViewBag.RolSortParm, currentFilter = ViewBag.CurrentFilter })
            </th>
            <th>
                @Html.ActionLink("Proyecto", "Index", new { sortOrder = ViewBag.RolSortParm, currentFilter = ViewBag.CurrentFilter })
            </th>
            <th>
                @Html.ActionLink("Solicitado por", "Index", new { sortOrder = ViewBag.RolSortParm, currentFilter = ViewBag.CurrentFilter })
            </th>
            <th>
                @Html.ActionLink("Crear Solicitud", "Index", new { sortOrder = ViewBag.ProySortParm, currentFilter = ViewBag.CurrentFilter })
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
           
            <tr id='@(item.versionRF+"~"+ item.idReqFunc+"~"+item.nomProyecto)' onclick="gotoVersionDetails(this)">
                <td>
                    @Html.DisplayFor(modelItem => item.versionRF )
                    
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ReqFuncional.nombre )
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.nomProyecto )
            </td>
                <td>
                    @{
                        var id = item.realizadoPor;

                        var userList = ViewBag.userList as IEnumerable<ProyectoInge1.Models.Usuario>;
                        foreach (var user in userList)
                        {
                            if (user.cedula.Equals(id))
                            {
                                var solicitante = user.nombre + " " + user.apellidos;
                                @Html.DisplayFor(modelItem => solicitante);
                            }
                        }

                }
                  
                </td>

                <td>
                    <div class="col-lg-2 col-lg-push-5">
                        @Html.ActionLink("Crear","Create",null,new{ nomProyecto = item.ReqFuncional.nomProyecto,versionRF = item.versionRF, idReqFunc= item.ReqFuncional.id },null)
                    </div>
                </td>

        </tr>
                    }
    </tbody>
</table>

<br />
Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount

@Html.PagedListPager(Model, page => Url.Action("Index",
    new { page, sortOrder = ViewBag.CurrentSort, currentFilter = ViewBag.CurrentFilter }))

<script src="../../Scripts/jquery_Usuarios.js"></script>


    @*

    @{var ProyectList = Model.listaProyectos;
        var Proyect = Model.Proyecto;

        foreach (var PList in ProyectList)
        {
            <option value=(PList.nombre>
                @Html.DropDownListFor(model => model.Proyecto, new[] { new SelectListItem() { Text = PList.nombre.ToString(), Value = PList.nombre } }, "--Proyecto no asignada--", new { @class = "form-control" })
            </option>


        }
    }

        @* @foreach (var item in Model)
        {
            <tr id='@(item.id)' onclick="gotoDetails(this)">
                <td>
                    @Html.DisplayFor(modelItem => item.nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.sprint)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.modulo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.estado)
                </td>
            </tr>
        }

    *@